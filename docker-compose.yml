version: '3.8'
services:
  docx-editor:
    build: .
    ports:
      - "3011:3011"
    volumes:
      - ./data/docs:/app/sync-server/docs
      - ./data/config:/app/sync-server/config
    environment:
      - PORT=3011
    restart: unless-stopped
