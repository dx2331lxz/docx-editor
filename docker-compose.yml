version: '3.8'
services:
  docx-editor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data/docs:/app/sync-server/docs
      - ./data/sessions:/app/sync-server/sessions
      - ./data/config:/app/sync-server/config
    environment:
      - PORT=3000
    restart: unless-stopped
